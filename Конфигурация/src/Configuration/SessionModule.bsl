&НаКлиенте
Процедура ПриНачалеРаботыСистемы()
    // Запрашиваем имя пользователя
    Имя = ВвестиСтроку("Введите ваше имя:");
    
    // Запрашиваем дату рождения
    ДатаРождения = ВвестиДату("Введите вашу дату рождения (ДД.ММ.ГГГГ):");
    
    // Получаем текущую дату
    Сегодня = ТекущаяДата();
    
    // Определяем дату рождения в текущем году
    ДеньРожденияВЭтомГоду = Дата(Год(Сегодня), Месяц(ДатаРождения), День(ДатаРождения));
    
    // Проверяем условия и выводим сообщение
    Если ДеньРожденияВЭтомГоду = Сегодня Тогда
        Сообщить("С днем рождения, " + Имя + "!");
    ИначеЕсли ДеньРожденияВЭтомГоду > Сегодня И ДеньРожденияВЭтомГоду <= Сегодня + 5 Тогда
        Сообщить(Имя + ", скоро ваш день рождения! Хорошо проведите праздник!");
    ИначеЕсли ДеньРожденияВЭтомГоду < Сегодня И ДеньРожденияВЭтомГоду >= Сегодня - 5 Тогда
        Сообщить(Имя + ", как прошел ваш день рождения?");
    Иначе
        Сообщить("Привет, " + Имя + "!");
    КонецЕсли;
КонецПроцедуры