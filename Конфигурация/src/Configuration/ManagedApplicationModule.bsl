//Функция ВвестиИмя()
//    ВвестиСтроку("Введите ваше имя:", ИмяПользователя);
//    Возврат ИмяПользователя;
//КонецФункции


//Процедура Приветствие()
//    ИмяПользователя = ВвестиИмя();
//    Сообщить("Привет, " + ИмяПользователя + "!");
//КонецПроцедуры


//АСИНХ Процедура ПриНачалеРаботыСистемы()
//	// Выводим окно для ввода числа
//	ВведенноеЧисло = ВвестиЧислоАсинх("Введите число от 0 до 1000");

//	Если ВведенноеЧисло = Неопределено Тогда
//	    ПредупреждениеАсинх("Вы не ввели!");
//	ИначеЕсли ВведенноеЧисло < 0 ИЛИ ВведенноеЧисло > 1000  Тогда
//	    ПредупреждениеАсинх("Вы ввели число не из диапозона. Вы проиграли!"); 
//	Иначе 
//		УгаданноеЧисло = ВвестиЧислоАсинх("Угадайте число от 0 до 1000");
//		Если ВведенноеЧисло > УгаданноеЧисло + 10 И  ВведенноеЧисло < УгаданноеЧисло - 10 Тогда
//			 ПредупреждениеАсинх("Вы не угадали число. Вы проиграли!");  
//		Иначе
//			 ПредупреждениеАсинх("Поздравляем, Вы выиграли!!");      
//		КонецЕсли; 
//	КонецЕсли;                                                 
//	
//КонецПроцедуры
  


//Процедура ПередНачаломРаботыСистемы(Отказ)
//// Определяем текущую дату
//ТекущаяДата = ТекущаяДата();

//// Получаем начало текущего года
//НачалоГода = НачалоГода(ТекущаяДата);

//// Инициализируем счетчик выходных
//КоличествоВыходных = 0;

//// Перебираем все дни от начала года до текущей даты
//ТекущийДень = НачалоГода;
//Пока ТекущийДень <= ТекущаяДата Цикл
//    // Проверяем, является ли текущий день субботой или воскресеньем
//    Если ДеньНедели(ТекущийДень) = 6 Или ДеньНедели(ТекущийДень) = 7 Тогда
//        КоличествоВыходных = КоличествоВыходных + 1;
//    КонецЕсли;

//    // Переходим к следующему дню
//    ТекущийДень = ТекущийДень + 86400; // Добавляем 1 день (86400 секунд)
//КонецЦикла;

//// Выводим результат
//Сообщить("Количество выходных дней (суббот и воскресений) с начала года: " + КоличествоВыходных);
//КонецПроцедуры

//Процедура ПередНачаломРаботыСистемы(Отказ)    
//// Определяем текущее время на компьютере
//ТекущееВремя = ТекущаяДата();

//// Задаем время окончания занятия (например, 15:30)
//ВремяОкончанияЗанятия = '2025-02-05 23:00:00'; // 15 часов 30 минут

//// Вычисляем разницу между текущим временем и временем окончания занятия
//РазницаВСекундах = ВремяОкончанияЗанятия - ТекущееВремя;

//// Если разница отрицательная, значит занятие уже завершилось
//Если РазницаВСекундах < 0 Тогда
//    Сообщить("Занятие уже завершилось");
//Иначе
//    // Вычисляем часы и минуты без использования деления
//    Часы = 0;
//    Минуты = 0;

//    Пока РазницаВСекундах >= 3600 Цикл
//        РазницаВСекундах = РазницаВСекундах - 3600;
//        Часы = Часы + 1;
//    КонецЦикла;

//    Пока РазницаВСекундах >= 60 Цикл
//        РазницаВСекундах = РазницаВСекундах - 60;
//        Минуты = Минуты + 1;
//    КонецЦикла;

//    // Выводим результат
//    Сообщить("До окончания занятия осталось: " + Часы + " часов " + Минуты + " минут");
//КонецЕсли;КонецПроцедуры  






